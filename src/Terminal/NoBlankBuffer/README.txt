This is an alternative to the Terminal Graphics which removes blanking buffers
It uses GrPixel() just like the SDL version does meaning it's mostly compatible
with SDL graphics addons like the 3D sdl render.c and not the terminal render.c
